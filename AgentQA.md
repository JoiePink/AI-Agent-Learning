AgentQA 的定位可以先记成一句话：

> 用户用自然语言描述测试目标，Agent 自动生成测试计划、操作 Web/微信小程序、验证结果、收集证据并生成测试报告。

## 一、核心使用流程

```
输入测试目标
→ Agent 生成测试计划
→ 用户确认或修改
→ Agent 操作页面
→ 自动验证测试结果
→ 收集截图和日志
→ 生成测试报告
```

## 二、核心功能

### 1. 项目与测试环境管理

用户可以创建测试项目并配置：

- 项目名称
- Web 或微信小程序
- 测试环境地址
- 测试账号
- 登录方式
- 环境变量
- 敏感信息加密保存

例如：

```
项目：商城管理后台
环境：https://test.example.com
账号：test-admin
```

### 2. 自然语言创建测试任务

用户不需要直接写 Playwright 代码，只需输入：

> 登录商城后台，创建一个售价99元的测试商品，确认商品出现在商品列表中。

系统将其转换成测试任务。

### 3. Agent 自动生成测试计划

Agent 输出结构化步骤，例如：

```
1. 打开登录页面
2. 输入账号密码
3. 点击登录
4. 进入商品管理
5. 点击新增商品
6. 填写商品名称和价格
7. 提交保存
8. 搜索刚创建的商品
9. 验证商品名称和价格
```

每一步包含：

- 操作对象
- 操作类型
- 输入数据
- 预期结果
- 风险等级

### 4. 测试计划确认与编辑

执行前允许用户：

- 修改步骤
- 删除步骤
- 调整顺序
- 修改测试数据
- 添加断言
- 选择从哪一步开始
- 保存为可重复使用的测试用例

这能防止 Agent 理解错误后直接执行。

### 5. Web 自动化执行

第一版使用 Playwright，支持：

- 打开网页
- 点击按钮
- 输入内容
- 选择下拉项
- 上传文件
- 页面跳转
- 等待元素出现
- 读取页面文本
- 获取表格内容
- 调用或监听接口
- 截图

### 6. 微信小程序自动化执行

后续通过微信开发者工具和 `miniprogram-automator` 支持：

- 启动小程序
- 切换页面
- 查找组件
- 点击、输入
- 读取页面数据
- 调用小程序方法
- 截图
- 验证页面状态

支付、摄像头、蓝牙、定位和真机兼容性等场景，仍需要真机或人工测试。

### 7. 智能元素定位

Agent 根据多种信息寻找目标元素：

- 按钮文字
- 标签名称
- `placeholder`
- `data-testid`
- CSS 选择器
- 页面结构
- 之前成功的定位记录

例如原来的“提交”按钮改成“保存”，Agent 可以尝试重新识别，而不是立即失败。

### 8. 自动断言

每一步操作后验证结果，例如：

- 页面是否跳转成功
- 是否出现“创建成功”
- 表格中是否存在目标数据
- 接口是否返回成功
- 表单错误提示是否正确
- 按钮是否处于禁用状态
- 页面是否出现控制台错误

断言应以确定性程序判断为主，模型判断为辅。

### 9. 失败诊断与恢复

测试失败时自动判断可能原因：

- 元素不存在
- 页面加载超时
- 选择器失效
- 接口请求失败
- 测试数据冲突
- Agent 操作错误
- 业务功能异常

允许 Agent：

- 重新定位元素
- 重试当前步骤
- 返回上一步
- 请求用户处理
- 保存状态后暂停
- 从失败步骤继续运行

### 10. 危险操作人工审批

遇到高风险操作时暂停：

- 删除数据
- 提交订单
- 退款
- 支付
- 修改权限
- 发布内容
- 操作生产环境

界面展示：

```
即将删除商品 TEST-001

风险等级：高
工具：delete_product
参数：{"productId":"TEST-001"}

[批准执行] [拒绝执行]
```

审批结果和操作人员需要被记录。

### 11. 实时执行时间线

前端通过 SSE 实时展示：

```
正在生成测试计划
正在打开登录页面
已经输入账号
登录成功
正在创建商品
等待人工审批
断言通过
测试执行完成
```

同时展示：

- 当前 Agent
- 当前步骤
- 工具调用
- 工具参数
- 工具结果
- Token 消耗
- 执行时间

### 12. 测试证据收集

自动保存：

- 步骤前后截图
- 页面 URL
- DOM 或页面结构
- 网络请求和响应
- 浏览器控制台日志
- Agent 工具调用
- 错误堆栈
- 人工审批记录

这样测试失败时有证据，不只是得到一句“执行失败”。

### 13. 测试报告

执行结束后生成标准报告：

- 测试目标
- 测试环境
- 测试步骤
- 成功和失败数量
- 每一步执行结果
- 实际结果与预期结果
- 失败原因
- 截图证据
- 接口错误
- Agent 执行轨迹
- 总耗时
- Token 和模型成本

支持在线查看和导出 HTML/PDF。

### 14. 测试用例管理

测试任务可以保存为用例，支持：

- 新建
- 编辑
- 复制
- 删除
- 分类
- 搜索
- 版本记录
- 手动运行
- 批量运行
- 查看历史结果

后期还可以加入定时回归和 CI/CD 触发。

### 15. Agent Trace 与执行回放

完整记录一次运行：

```
模型为什么生成这个步骤
调用了哪个工具
工具收到什么参数
工具返回什么结果
为什么决定重试
为什么判断测试失败
```

用户可以按照时间线回放整个执行过程。

### 16. Evals 评测系统

用固定题库持续评估 Agent：

- 测试计划是否合理
- 是否遗漏关键步骤
- 工具选择是否正确
- 参数是否正确
- 危险操作是否触发审批
- 页面变化后能否恢复
- 最终判断是否准确
- 不同模型的成功率、成本和速度

这是 AgentQA 区别于普通 AI Demo 的关键能力。

## 三、第一版 MVP 只做这些

先不要同时开发全部功能。第一版完成下面这条闭环就已经成立：

1. 创建一个 Web 测试项目。
2. 输入自然语言测试目标。
3. Agent 生成结构化测试计划。
4. 用户确认或编辑计划。
5. Playwright 操作网页。
6. 执行页面和接口断言。
7. 危险操作请求人工审批。
8. 前端实时展示执行过程。
9. 自动保存截图和错误日志。
10. 生成一份 HTML 测试报告。
11. 保存任务状态，支持中断后继续。
12. 准备10～20个固定用例评测成功率。

第一版暂时不做：

- 微信小程序
- 多租户
- 复杂权限系统
- 收费系统
- 大规模并发
- 多浏览器云测试
- 知识库/RAG
- 手机真机测试

## 四、简历上最有价值的四个亮点

最终一定要重点展示：

1. **Agent 真的操作了页面**，不是只生成测试文案。
2. **执行过程可控制**，有审批、状态恢复和失败重试。
3. **测试结果可信**，有断言、截图、日志和完整 Trace。
4. **效果可以量化**，有 Evals、成功率、耗时和成本对比。

你在地铁上可以重点思考一个问题：

> 如果只能选择一个真实网站作为 AgentQA 的第一个测试对象，你最熟悉、也最容易准备测试账号和测试数据的是哪个系统？

这个测试对象会直接决定第一版 MVP 的具体功能。